{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the axio window's webview may do. Deliberately almost nothing: every capability here is one a script running in the webview also gets, so anything that can be a typed command is one.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open"
  ]
}
